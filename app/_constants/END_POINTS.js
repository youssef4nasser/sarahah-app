const BASE_URL = 'https://sarahaa-0.onrender.com'

// AUTH URLs
const BASE_AUTHS = `${BASE_URL}/auth`;

export const AUTH_URLS = {
    login: `${BASE_AUTHS}/Login`,
    signup: `${BASE_AUTHS}/signup`,
}
// ======================================================================
